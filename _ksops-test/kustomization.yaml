apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# KSOPS generator: decrypts *.enc.yaml files referenced by ./ksops.yaml
# and emits them as kustomize resources.
generators:
  - ./ksops.yaml
